# Phase 4 — MLflow (local) instructions

## Purpose
Log training runs, metrics, and artifacts locally with MLflow.

## Install
Make sure MLflow is installed in your project venv:
```bash
source .venv/bin/activate
pip install mlflow
