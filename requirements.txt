# ============= Phase 1 dev deps (kept) =============
pytest>=7.0
pytest-cov>=4.0
flake8>=6.0
black>=24.0
isort>=5.12
mypy>=1.9
types-requests
types-PyYAML
python-dotenv>=1.0

# ============= Phase 2 runtime deps ============
pandas>=2.0
pyarrow>=12.0   # used for parquet feature store

fastapi
uvicorn
